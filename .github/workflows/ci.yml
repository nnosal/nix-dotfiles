name: Nix Flake CI

on:
  push:
    branches: [main, jules-ultimate-dotfiles-init-*]
  pull_request:

jobs:
  # ========================================
  # JOB 1: Validation Syntaxique
  # ========================================
  validate:
    name: "🔍 Flake Check & Linters"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: cachix/install-nix-action@v25
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
      
      - uses: cachix/cachix-action@v15
        with:
          name: nnosal-dotfiles
          authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
      
      # 1. Validation Nix Flake
      - name: Nix Flake Check
        run: nix flake check --show-trace
      
      # 2. Validation Mise (syntaxe uniquement)
      - name: Validate mise.toml
        run: |
          nix shell nixpkgs#mise -c bash -c '
            echo "📋 Vérification de mise.toml..."
            
            # Parse la config
            mise config ls > /dev/null || exit 1
            
            # Liste les tasks
            echo "Tasks définies:"
            mise tasks ls
            
            # Vérifie les tools déclarés
            echo "Tools déclarés:"
            mise ls --json | head -20
            
            echo "✅ mise.toml est valide"
          '
      
      # 3. Vérification marqueurs injection
      - name: Validate Injection Markers
        run: |
          ERROR=0
          
          if [ -f modules/darwin/apps.nix ]; then
            if ! grep -q "# %% CASKS %%" modules/darwin/apps.nix; then
              echo "❌ Marker CASKS manquant dans modules/darwin/apps.nix"
              ERROR=1
            fi
          fi
          
          if [ -f modules/common/packages.nix ]; then
            if ! grep -q "# %% PACKAGES %%" modules/common/packages.nix; then
              echo "❌ Marker PACKAGES manquant dans modules/common/packages.nix"
              ERROR=1
            fi
          fi
          
          if [ $ERROR -eq 0 ]; then
            echo "✅ Tous les marqueurs d'injection sont présents"
          else
            exit 1
          fi

  # ========================================
  # JOB 2: Build Configurations
  # ========================================
  build-configs:
    name: "🔨 Build ${{ matrix.name }}"
    runs-on: ${{ matrix.os }}
    needs: validate
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: "macOS (macbook-pro)"
            config: darwinConfigurations."macbook-pro".system
            os: macos-14
            
          - name: "Linux VPS (contabo1)"
            config: nixosConfigurations.contabo1.config.system.build.toplevel
            os: ubuntu-latest
            
          - name: "Linux Test"
            config: nixosConfigurations.test-linux.config.system.build.toplevel
            os: ubuntu-latest
            
          - name: "WSL (Home Manager)"
            config: homeConfigurations.wsl.activationPackage
            os: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: cachix/install-nix-action@v25
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
      
      - uses: cachix/cachix-action@v15
        with:
          name: nnosal-dotfiles
          authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
      
      - name: Build Configuration
        run: |
          echo "🔨 Building ${{ matrix.name }}..."
          nix build .#${{ matrix.config }} --dry-run --show-trace

  # ========================================
  # JOB 3: Security Audit
  # ========================================
  security:
    name: "🔒 Security Audit"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: cachix/install-nix-action@v25
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
      
      - name: Detect Private Keys & Secrets (using hk)
        run: |
          echo "🔍 Scanning for hardcoded secrets using hk..."
          
          # Build hk from flake
          nix build .#hk
          
          # Run security scan with hk
          ./result/bin/hk scan . \
            --exclude .git \
            --exclude result \
            --exclude '*.md' \
            --format github-actions
          
          echo "✅ Security scan completed"
      
      - name: Validate Fnox Config (Zero-Trust)
        run: |
          if [ -f fnox.toml ]; then
            echo "🛡️ Validating fnox.toml (must contain pointers only)..."
            
            # Vérifier qu'il n'y a que des URIs keychain:// ou pass://
            if grep -E '^\s*[A-Z_]+\s*=\s*"' fnox.toml | grep -v 'keychain://' | grep -v 'pass://'; then
              echo "❌ ERROR: fnox.toml contains values that are not keychain:// or pass:// URIs!"
              echo "All secrets must be pointers, not actual values."
              exit 1
            fi
            
            echo "✅ fnox.toml follows Zero-Trust principle (pointers only)"
          else
            echo "⚠️ fnox.toml not found (OK if not created yet)"
          fi
